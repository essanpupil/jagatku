# Disable components not needed on edge agent clusters
alertmanager:
  enabled: false
grafana:
  enabled: false
kubeStateMetrics:
  enabled: true
nodeExporter:
  enabled: false
prometheusOperator:
  enabled: false

prometheus:
  agentMode: true
  prometheusSpec:
    disableAlertmanager: true
    ruleSelector: {}
    ruleNamespaceSelector: {}
    remoteWrite:
      - name: "localPrometheus"
        url: "http://prometheu.laptop1.local/api/v1/write"
    scrapeInterval: "30s"
    evaluationInterval: "30s"
    resources:
      limits:
        cpu: "500m"
        memory: "1Gi"
      requests:
        cpu: "200m"
        memory: "512Mi"
    retention: "10m"
