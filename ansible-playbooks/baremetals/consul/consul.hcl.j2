datacenter = "jagatku"
data_dir = "/opt/consul"

server = true
bootstrap_expect = 1
retry_join = ["127.0.0.1"]
bind_addr = "{{ ansible_host }}"
client_addr = "0.0.0.0"

ui_config {
  enabled = true
}
